# Configuração do Servidor
HOST=0.0.0.0
PORT=8080

# Configuração da Blockchain
RPC_URL=https://sepolia.infura.io/v3/SEU_INFURA_KEY
CHAIN_ID=11155111
PRIVATE_KEY= # (Nunca use sua chave privada real em produção/testnet pública, use uma wallet de teste)

# Endereços dos Contratos (preencha após deploy na testnet)
ASSET_TOKEN_ADDRESS=0xSEU_ASSET_TOKEN
MARKETPLACE_ADDRESS=0xSEU_MARKETPLACE
WAITLIST_ADDRESS=0xSEU_WAITLIST

# Configuração do Banco de Dados
DATABASE_URL=postgres://usuario:senha@localhost:5432/tokeniza

# Configuração de Logs
RUST_LOG=info

# Ambiente
RUST_ENV=testnet

# Chave secreta para JWT ou sessões (substitua por uma chave forte em produção)
SECRET_KEY=algumasecretkeysegura
