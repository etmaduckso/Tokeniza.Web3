# Exemplo de .env para o deploy dos contratos (onchain)

# RPC da testnet (Sepolia, Goerli, etc)
RPC_URL=https://sepolia.infura.io/v3/SEU_INFURA_KEY
CHAIN_ID=11155111

# Chave privada da wallet de deploy (use apenas para testes, nunca use sua chave real)
PRIVATE_KEY=0xSUA_PRIVATE_KEY_TESTE

# Etherscan/Blockscout API Key (opcional, para verificação de contrato)
ETHERSCAN_API_KEY=

# (Adicione outras variáveis conforme necessário)
